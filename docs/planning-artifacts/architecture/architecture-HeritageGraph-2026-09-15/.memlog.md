---
scope: HeritageGraph toàn hệ thống
purpose: architecture spine — bất biến giữ các đơn vị độc lập không phân kỳ
altitude: hệ thống
updated: 2026-09-17T00:25
---

- (decision) Paradigm: hexagonal (ports & adapters) over layered core — api/ la driving adapters, core/ la domain thuan (retrieval pipeline + KG + prompt contract), corpus/ + models/ + external APIs la driven adapters. LLM dung sau port co 3 backend (mlx/llama_server/llama_cpp).
- (decision) [ADOPTED] Retrieval pipeline: BM25 tu + BM25 char-4-gram + KG rerank; RRF k=60; GRAPH_WEIGHT=0.35; NAMED_DOC_BONUS=0.8; CHUNK_ENTITY_BONUS=0.15; CANDIDATES=30; INJECT_PER_DOC=5. Retrieval /api/chat giu trong RAM (lru_cache get_retriever), p95 <50ms.
- (decision) [ADOPTED] Cong tu choi: REQUIRE_GRAPH_ANCHOR + MIN_COVERAGE=0.25 + REQUIRE_EVIDENCE_FOR_NAMED + REQUIRE_SUBJECT_EVIDENCE + REQUIRE_KNOWN_ADMIN. Chan -> context trong -> model tu choi theo mau train. Context = <=2 chunk noi lien, <=2200 ky tu, giu thu tu trong bai.
- (decision) [ADOPTED] KG deterministic, KHONG LLM trong trich xuat (FR04/FR05). Node id kind:value (doc:/entity:/region:/category:/year:). Edge closed vocab co trong so: is_about 3.0, in_region/in_category 1.0, in_ward 2.0, mentions <=5, year <=3, related <=5.
- (decision) [ADOPTED] Chunker duy nhat: backend/core/corpus.py (MAX_CHUNK 1200/MIN 200) dung chung train+serve. Chunk id '<ten bai>#<i>', #0 la lead chunk. ingestion/chunk_corpus_mt.py (700/80) + graphrag/input legacy — xoa.
- (decision) [ADOPTED] Prompt contract: citation '[Nguồn: <cau nguyen van> — <url>]'; user_msg 'Nguồn: {source|(khong co)}\n\nCâu hỏi: {question}'; temperature=0; MAX_TOKENS=768. Context la MOT doan lien mach (khong ky hieu la) vi mau train co dang do.
- (decision) [ADOPTED] Serve base+adapter (models/lora-serve), KHONG dung fused — requantize 4-bit khi fuse dao 3/5 cau tieu cuc (chi 1/5 cho base+adapter). models/qwen-fused chi cho training/bootstrap.
- (decision) [ADOPTED] Degradation thay vi crash: loi retrieval -> context trong -> refusal da train; HTTPException detail tieng Viet; fail-fast RuntimeError luc import cho nguy co silent corruption (kg.py, nerlabel.py).
- (decision) Persistence: schema.sql v2 (16 bang) la binding persistence spine; bat pgvector cho passage_embedding vector(1024). Bang PRD moi (app_user argon2id, user_interest, interaction_event, recommendation_log, venue, event, event_segment, artifact, poi, media_asset) cung lop + quy uoc. Tang DB hien CHI CO bytecode cu — xay moi.
- (decision) Id convention: TEXT '<PREFIX>-<NNNNNN>' (DOC-/PSG-/ENT-/REL-/VEN-/EVT-/ART-) cho bang lop tri thuc — human-citable cho NFR13; BIGSERIAL cho bang log (audit_log, chat_message, interaction_event). schema.sql dung UUID -> supersede.
- (decision) Checkpoint serve: 0000200 (val loss 0.414, NER micro-F1 0.7861). models/lora-serve/CHECKPOINT la single source of truth; fix drift (dang ghi 0000400).
- (decision) Binding: demo local bind 127.0.0.1 (NFR11 — sua config.py BACKEND_HOST chet); container bind 0.0.0.0 internal, port mapping kiem soat exposure.
- (decision) Test: backend giu unittest (stdlib) — reality; E2E moi dung Playwright; eval harness (eval/) doc lap voi metadata SHA-256 (gold/prompt/corpus/adapter/scorer) — co che NFR16.
- (decision) Card registry (FR18): moi truong the = truong ban ghi nguon hoac phan hoi API co ten; assert tu dong 0 truong bia (NFR06); the mang provenance + fetched_at. The moi chi dang ky bo sinh (NFR17).
- (decision) Privacy: interaction_event chi ghi SAU consent thong bao (NFR12); /api/me/export + /api/me/data khong the bi cat.
- (constraint) NFR14: moi bang ban ghi (venue/event/event_segment/artifact/media_asset) NOT NULL source_url + source_sentence o muc DB, khong phai app.
- (assumption) [ASSUMPTION] Tang DB hoan toan vang mat hom nay (common/db/services/models chi con .pyc); spine dac ta xay moi theo schema.sql v2 + bang PRD.
- (question) Vector channel (FR12): pgvector dim 1024 qua mlx-embeddings; tham gia khi 'coverage >= MIN_COVERAGE HOAC cosine >= theta'. theta chua co so — can hieu chinh tren OUT_OF_DOMAIN Sprint 3; danh doi NFR04 vs NFR07 bao cao cung bang.
- (question) So dia diem curated khong nhat quan: 49 (kg.py docstring) vs 47 (locations_index.json) vs 45 (locations_hue_danang.py). Can mot index duy nhat.
- (question) report_base.json va report_base_76.json byte-identical (135,275 B) — trung lap.
- (decision) Frontend ratified: Next.js 14.2 + @ant-design/x 2.9, XProvider darkAlgorithm (colorPrimary #FA500F, colorBgBase #000, radius 10, Inter). @assistant-ui/* co trong package.json nhung khong import — drop. Frontend khong goi /api/graph/* (chi chat).
- (decision) Stale cleanup: xoa .bak, graphrag/prompts rong, frontend/lib rong, backend/models/qwen2.5-7b legacy, backend/package-lock.json (Python project), metadata.ts branding 'Recruitify' sai.
- (decision) docs/chatbot.md la tai lieu chuan cho kien truc chatbot: Domain GraphRAG/KAG-lite (MS GraphRAG provenance + Local Context + KAG query planner/operators), PostgreSQL lam source of truth, Qwen3-4B generation, KHONG agent framework/Neo4j/OpenSPG runtime.
- (decision) Model: target Qwen3-4B + LoRA (models/peft-adapter/, eval_loss 0.3495). As-built dang serve Qwen2.5-3B + lora-serve ckpt 0000200 (llm.py load BASE_MODEL mlx-community/Qwen2.5-3B-Instruct-4bit). Migration = viec Epic 2 nhánh A.
- (decision) LoRA chi day CACH TRA LOI (style/citation/refusal), KHONG bao gio tham gia retrieval/vector. Retrieval la query planner + operators deterministic (exact claim / timeline / graph 1-2 hop / section / text hybrid).
- (decision) AD-2 doi: pipeline 3 kenh co dinh → query planner + 5 operators theo intent (8 lop: overview/field/timeline/relationship/section/comparison/open_text/out_of_scope). BM25x2+RRF+graph as-built = Text+Graph operator core giu lai.
- (decision) AD-3 doi: 3 cong lexical → 2 lop gate (pre-generation: in_scope/entry_status/corpus_version/evidence du; post-generation: citation ID thuoc evidence package, so/ngay co trong evidence). Response 1 trang thai: answered/clarify/abstained.
- (decision) AD-6 doi: target citation ID do Context Builder cap ([P17]... Cau hoi:...); as-built [Nguồn: <cau> — <url>] giu cho Qwen2.5 den khi migrate.
- (decision) Vector channel: chatbot.md dat CO DIEU KIEN — 'dense vector chi neu eval chung minh co loi'. Spike truoc (dense top-3 >=70% tren PARAPHRASE) moi quyet xay.
- (decision) Module mapping (chatbot.md §14): services/kg.py = entity resolution; core/ = intent router + query planner moi; api/ chi validate/serialize — khong chua SQL/retrieval/planning/prompt/citation validation.
